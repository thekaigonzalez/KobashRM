local f = {}

function f:main(a)
    if #a == 1 then
        print("installer utility\nTo install, type the name of a package here (example, installutility <modname> <installscript>")
        print("if <installscript> isn't supplied, it will default to the COPY (default copy script) method.")
        print("this utility is used as a way to custom install commands using their own ")
    end
end

return f